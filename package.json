{
  "name": "umechan-opti-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @umechan/shared run build && concurrently \"pnpm --filter @umechan/shared run dev\" \"pnpm --filter epds run dev\" \"pnpm --filter umechan-opti run dev\"",
    "build": "pnpm -r run build",
    "lint": "pnpm --filter umechan-opti run lint && pnpm --filter epds run lint",
    "test": "pnpm --filter epds run test"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
